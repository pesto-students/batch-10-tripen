const destructureUser = ({
  _id,
  name,
  username,
  email,
  location,
  createdAt,
  updatedAt,
}) => ({
  _id,
  name,
  username,
  email,
  location,
  createdAt,
  updatedAt,
});

export default destructureUser;
